SESSION_SECRET=replace_with_random_string_at_least_32_chars
ADMIN_PASSWORD_HASH=scrypt:replace_salt_hex:replace_hash_hex
# Optional (dev only fallback, not recommended in production):
# ADMIN_PASSWORD=change_me
